import React from 'react';
import { SignupFormDemo }from '../app/components/ui/Signup'; // Adjust the path to where `SignupFormDemo` is located

const LoginPage: React.FC = () => {
  return (
    <div>
      <SignupFormDemo />
    </div>
  );
};

export default LoginPage;
